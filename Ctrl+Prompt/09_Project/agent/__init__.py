"""Disney Agent Support Tickets — CLI-first root-cause triage agent.

Stdlib-only implementation so the demo runs with zero installs.
Production stack target (see README): Typer + Amazon Bedrock + scikit-learn.
"""

__version__ = "0.1.0"
